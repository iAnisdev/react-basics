import { Component, Fragment } from "react";

export class ErrorBoundarySub extends Component {
    render() {
        return (
            <Fragment>
                <p>Should be rendering without error</p>
            </Fragment>
        )
    }
}